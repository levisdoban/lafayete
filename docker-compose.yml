
version: '3.8'

services:
  lafayetteapp:
    container_name: lafayetesite
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5044:80"
    environment:
      - NODE_ENV=production
